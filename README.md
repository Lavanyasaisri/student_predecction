# AI Student Performance Predictor

This project predicts student performance based on various factors using machine learning.

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Train the model: `python train_model.py`
3. Run the app: `python app.py`

## Dataset

The dataset is located in `dataset/student_performance_data.xlsx`.